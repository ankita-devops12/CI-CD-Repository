name: Hello
on: 
 push:
   branches: [main]
jobs:
  greet:
   runs-on: ubuntu-latest
   steps:
     - name: Say hello to user
       run: echo "Hello Dosto"
     - name: Say hello to user name
       run: echo "Hello $USER"
